# Stage contract schema — example

This package conforms to `workflow-contract/0.1.0`.

In the authoring repository, the canonical schema is
`../../../../_config/workflow-contract-schema.md`. An exported copy records
its origin and version; copies are never edited as peers of the canonical
file.
