# Fixture: unsupported interview format — no interview-evidence blocks.
# Must produce an explicit diagnostic, never be interpreted as owner facts.

transcript:
  - "we usually just merge"
  - "deploys happen fridays"
